#pragma once
#include <Actualizable.hpp>
#include <curses.h>

class Controlador : public Actualizable
{
private:
    /* data */
public:
    Controlador(/* args */) {}
    ~Controlador() {}
    void Actualizar()
    {
        // Ciclo de actualizacion
        // v.Actualizar();
        // if(getch()=='q'){
        //   ejecucion = false;
        // }
        // if(getch()=='a'){
        //   d2.RetrocederX(1);
        // }

        // if(getch()=='d'){
        //   d2.AvanzarX(1);
        // }
    }
};